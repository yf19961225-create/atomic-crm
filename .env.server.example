# Server-only: configure in Vercel project settings; locally use ignored .env.local.
# Never prefix these names with VITE_, commit their values, or give them to browsers.
WEBSITE_INQUIRY_SECRET=
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
